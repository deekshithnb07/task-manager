import React, { useState } from "react";
export default Action;

function Action() {
  const [countServer, setCountServer] = useState(0);

  return (
    <div>
      <h1>Action</h1>
    </div>
  );
}
